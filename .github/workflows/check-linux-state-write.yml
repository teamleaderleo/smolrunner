name: Check Linux state write

on:
  push:
    branches: [feature/linux-state-write]

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Check out branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: feature/linux-state-write
          fetch-depth: 1

      - name: Format and capture compiler diagnostics
        shell: bash
        run: |
          set +e
          cargo check --all-targets --all-features --message-format=short > .linux-state-write-check.txt 2>&1
          status=$?
          printf '\nexit_status=%s\n' "$status" >> .linux-state-write-check.txt
          cargo fmt --all
          rm -f \
            .github/workflows/check-linux-state-write.yml \
            .tmp/check-linux-state-write
          rmdir .tmp 2>/dev/null || true
          git diff --check
          exit 0

      - name: Commit checked branch
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'build: check atomic Linux state writes'
          git push origin HEAD:feature/linux-state-write
